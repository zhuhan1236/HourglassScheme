整个实验分为客户端和服务器两部分，运行步骤如下：
     服务器：首先，打开配置文件serverConfig.txt，可以看见里面有三行：
                     第一行是服务器的工作目录的绝对路径，用来模拟云盘。
                     第二行是控制连接监听的端口（数据连接监听的是4081号端口，请保证畅通）
                     第三行是服务器的工作模式（0为优质服务商，1为不良服务商）

     客户端：首先，打开配置文件clientConfig.txt，可以看见里面有三行：
                     第一行是客户端的工作目录的绝对路径，用来存放从云端下载的文件
                     第二行是服务器的IP地址
                     第三行是服务器端控制连接监听的端口



配置文件修改完成后，先运行服务器，再运行客户端。我们的客户端支持如下几种命令：
stor <file full path>                   此命令能上传文件到服务器
list                                    此命令能列出服务器工作目录下的文件
retr <file name>                        此命令获取服务器端文件，存储到客户端
geth <file name>                        此命令获取加密后的文件G，然后在客户端运行Hourglass函数获取H文件，存储在客户端工作目录下
chal <block number i> <file full path>  此命令使用challange-response协议询问H文件的第i块
                     
